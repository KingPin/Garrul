{
  "name": "garrul",
  "version": "0.0.1",
  "private": true,
  "description": "Self-hosted comment system on Cloudflare Workers + D1 + KV",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "predev": "npm run build:embed",
    "deploy": "wrangler deploy",
    "predeploy": "npm run build:embed",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "prebuild": "npm run build:embed",
    "build:embed": "tsx scripts/build-embed.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.widget.json",
    "pretypecheck": "npm run build:embed",
    "lint": "echo 'lint placeholder — wire up biome or eslint later' && exit 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "tsx src/db/migrate.ts",
    "rerender": "tsx scripts/rerender.ts",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "marked": "^18.0.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20251101.0",
    "@types/node": "^25.8.0",
    "esbuild": "^0.28.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
